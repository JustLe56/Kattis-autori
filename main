#include <iostream>
#include <string>
using namespace std;
int main() {
  string userInput;
  cin >> userInput;
  cout << userInput[0];
  for (int i = 0;i < userInput.size();i++)
  {
    
    if (userInput[i] == '-')
    {
      cout << userInput[i+1];
    }
  }
}
